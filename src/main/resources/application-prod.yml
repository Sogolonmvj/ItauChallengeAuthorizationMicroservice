spring:
  datasource:
    password: 0wN3r
    url: jdbc:postgresql://critics-system-postgres:5432/critics_registration
    username: postgres
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true